"use client";

import { DocsShell } from "@/components/purvex-landing-page/docs-shell";

export default function InstallGuideLayout({ children }: { children: React.ReactNode }) {
  return <DocsShell>{children}</DocsShell>;
}
